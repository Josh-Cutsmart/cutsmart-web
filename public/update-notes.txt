Version: v0.4.2

whatsNew: [
<u><b>Sidebar</b></u>
<ul>
<li>Main tabs reordered: <b>Dashboard, Leads, Recently Deleted, Clients, Calendar, Company Wrapped, Company Settings</b></li>
<li><b>Changelog</b> moved to the bottom of that list, separated by a divider line</li>
</ul>

<u><b>Changelog</b></u>
<ul>
<li>The dev-only version editor is back to a plain HTML textarea with a helpers panel, instead of the rich-text editor:
  <ul>
  <li>Each helper now shows its actual HTML tag next to the label, with a copy button to copy it straight to your clipboard</li>
  <li>Clicking a helper no longer jumps the editor's scroll position — it only moves if the cursor it just placed is actually off-screen</li>
  </ul>
</li>
<li>Version dates now use the shorter month format (e.g. <b>25 Sep 2026</b>), matching the rest of the app</li>
<li>Each version's date is now shown on the right side of its own header bar</li>
</ul>

<u><b>Dashboard</b></u>
<ul>
<li>Column (board) view no longer shows the All/Active/Completed filters on desktop — the columns already separate complete from active by status, so every column always shows everything</li>
<li>Nudged the search icon over slightly in the search bar</li>
</ul>
]
