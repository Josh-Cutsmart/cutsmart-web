"use client";

export default function CompanyUpdatesPage() {
  return (
    <section className="rounded-[14px] border border-[var(--panel-border)] bg-[var(--panel-bg)] p-4">
      <p className="text-[17px] font-semibold text-[var(--text-main)]">Company Updates</p>
      <p className="mt-2 text-[13px] text-[var(--text-muted)]">Company activity page route is now in place for desktop-parity content wiring.</p>
    </section>
  );
}
